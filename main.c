#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a,b;
    printf("Enter the two number's :");
    scanf("%d %d",&a ,&b);

    if(a<b){
       printf("The smaller number is: %d",a);

    }else{
        printf("a is getter then b");

   }

    return 0;
}
